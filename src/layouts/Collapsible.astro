---
export interface Props{
    sumaryText: string;
};

const {sumaryText} = Astro.props;

---
<details class="m-5 p-2 shadow-md bg-gray-100 [&>p]:text-justify [&>p]:mb-3">
    <summary  class="cursor-pointer text-xl">{sumaryText}</summary>
    <slot />
</details>

